#include "main.h"

/**
 * _abs - computes the absolute value of integer
 * @n: the intager to check
 * Return: the absolute value of intager.
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

