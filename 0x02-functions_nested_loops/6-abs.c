#include "main.h"


	/**
	 * _abs - this computes the absolute value of an integer
	 * @n: is the int to check
	 * Return: the absolute value of int in the end
	 */
	int _abs(int n)
	{
		if (n >= 0)
		{
			return (n);
		}
		return (-n);
	}

